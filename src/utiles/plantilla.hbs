<div style="font-family: Arial, Helvetica, sans-serif;">
  <h2>{{titulo}}</h2>
  <p>{{saludo}}</p>
  <p>{{cuerpo}}</p>

  {{#if boton_url}}
    <p style="text-align:center; margin:16px 0;">
      <a href="{{boton_url}}"
         style="display:inline-block; padding:10px 16px; background:#0d6efd; color:#fff; 
                text-decoration:none; border-radius:6px; font-weight:600;">
        {{#if boton_texto}}{{boton_texto}}{{else}}Abrir enlace{{/if}}
      </a>
    </p>
    <p style="font-size:12px; color:#666;">
      Si el botón no funciona, copiá y pegá este enlace en tu navegador:<br>
      <span style="word-break:break-all;">{{boton_url}}</span>
    </p>
  {{/if}}

  {{!-- ✅ Si viene contrasenia_temporal entonces NO mostrar fecha/salon/turno --}}
  {{#if contrasenia_temporal}}
    <p>Tu contraseña temporal es: <strong>{{contrasenia_temporal}}</strong></p>
    <p>Recordá cambiarla apenas ingreses desde tu perfil.</p>
  {{else}}
    <ul>
      <li><b>Fecha:</b> {{fecha}}</li>
      <li><b>Salón:</b> {{salon}}</li>
      <li><b>Turno:</b> {{turno}}</li>
      {{#if nombre}}
        <li><b>Cliente:</b> {{nombre}}</li>
      {{/if}}
    </ul>
  {{/if}}

  <p>{{pie}}</p>
</div>


